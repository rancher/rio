services:
  cert-manager:
    disableServiceMesh: true
    global_permissions:
    - '* certmanager.k8s.io/*'
    - '* extensions/ingresses'
    - '* configmaps'
    - '* secrets'
    - '* events'
    - '* services'
    - '* pods'
    image: daishan1992/cert-manager:latest
    imagePullPolicy: Always
    args:
    - cert-manager
    - --leader-election-namespace
    - ${NAMESPACE}
    - --cluster-resource-namespace
    - ${NAMESPACE}
    - --logtostderr
    - "true"

