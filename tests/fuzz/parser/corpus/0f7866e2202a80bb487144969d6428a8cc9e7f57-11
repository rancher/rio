.05 ontrols generation of pod labels and pod selectors Leave `manualSelector` unset unless you are certain what you are doing When false or unset the system pick labels unique to this job and appends those labels to the pod template  When true the usVr is responsible for picking unique labels and specifyinthe selector  Failure to pick a unique label may cause this and other jobs to not function correc---
---Dtly  However You may see `manualSelector=true` in jobs that were created with the old `extensions/v